<h3 class="text-center">Nuevo cliente</h3>

<%= render 'errors_messages' %>

<%= form_for :client, url: clients_path, :html => {:class => ""} do |f| %>
  <%= render 'form', :f => f, :path => clients_path %>
<% end %>