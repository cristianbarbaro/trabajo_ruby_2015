<h1 style="text-align:center">Nuevo cliente</h1>

<%= render 'errors_messages' %>

<%= form_for :client, url: clients_path, :html => {:class => "col-md-6 col-md-offset-3"} do |f| %>
  <%= render 'form', :f => f %>
	<%= link_to 'Atrás', clients_path, class: 'btn btn-danger pull-right' %>
<% end %>